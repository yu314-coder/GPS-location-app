# Recording a route without satellites

Paper describing the vibration-derived speed method used by Velocity Mode, and its
measured accuracy and failure modes across 23 instrumented journeys.

    cd paper && pdflatex velocity_mode.tex && pdflatex velocity_mode.tex

`figures/` is regenerated by the plotting block in the project history; the values are
measured, not modelled.
